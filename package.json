{
  "name": "switch-price-monitor",
  "private": true,
  "version": "0.0.17",
  "type": "module",
  "codexMetadata": {
    "databaseDependencyRationaleZh": {
      "pg": "pg 是 Node 服务连接 PostgreSQL 的生产运行时依赖，不能放入仅测试/构建使用的 devDependencies。",
      "parameterization": "所有动态 SQL 值必须通过 pg 参数绑定传入，禁止拼接认证、价格来源、筛选条件或其他外部输入。",
      "credentials": "数据库连接凭据只从经过验证的运行时配置注入，不得写入源码、测试产物、镜像层或普通日志。"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "node ./scripts/deploy-production.mjs",
    "test": "vitest",
    "test:dom": "vitest --config vitest.dom.config.mts"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@cloudflare/playwright": "1.3.0",
    "@cloudflare/vite-plugin": "^1.17.1",
    "@cloudflare/vitest-pool-workers": "^0.18.5",
    "@cloudflare/workers-types": "5.20260714.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/pg": "^8.20.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.2",
    "jsdom": "^29.1.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.6",
    "vitest": "^4.1.0",
    "wrangler": "4.112.0"
  }
}
