# 复制为同目录未提交的 `.env` 后再替换；本文件只有公开占位，绝不能粘贴真实密码、令牌、恢复码或会话值。

# Docker Hub 公开仓库和精确语义版本共同定位不可变发布；NAS 生产部署禁止使用 latest。
DOCKERHUB_IMAGE=your-dockerhub-user/switch-price-monitor
APP_VERSION=0.1.0

# APP_PORT 是 NAS 对局域网发布的端口，PORT 是容器内 Node HTTP 端口；两者均不得改成 PostgreSQL 或 Chromium 调试端口。
APP_PORT=3000
PORT=3000

# POSTGRES_* 是官方镜像只在数据库容器内部使用的 bootstrap 管理角色；不得写入 DATABASE_URL 或传给 app。
# APP_DATABASE_* 是 init hook 创建的普通项目所有者。两种角色必须使用不同随机长密码，用户名也不得相同或使用 postgres。
POSTGRES_DB=switch_price_monitor
POSTGRES_USER=switch_price_bootstrap
POSTGRES_PASSWORD=replace_with_random_admin_password
APP_DATABASE_USER=switch_price_monitor
APP_DATABASE_PASSWORD=replace_with_random_app_password

# DATABASE_URL 只能使用 APP_DATABASE_USER 与同一应用密码的 URL 编码结果；若密码包含 @、:、/、% 等字符必须逐字节百分号编码。
DATABASE_URL=postgresql://switch_price_monitor:replace_with_same_url_encoded_password@postgres:5432/switch_price_monitor

# 数据目录必须是 NAS 上已创建、权限受控的绝对项目目录；不要指向现有 PostgreSQL 项目或仓库目录。
POSTGRES_DATA_DIR=/absolute/path/to/switch-price-monitor/postgres-data

# 当前局域网纯 HTTP 必须显式为 false；未来经 HTTPS/FRP 公开前必须改为 true，应用不会根据请求头自动降级。
COOKIE_SECURE=false

# Telegram 两项必须同时为空或同时填写；普通日志、Compose 健康检查和镜像层都不得输出它们。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# AI_CREDENTIAL_ENCRYPTION_KEY 是 Node 进程私有的 AES-256-GCM 主密钥；在可信本机以 `openssl rand -base64 32` 生成后填入未提交且权限为 600 的 .env。
# 它只能传给 app，绝不能复制到浏览器、PostgreSQL、镜像、日志、备份、截图或工单；丢失或更换后旧 AI 配置无法解密，须在设置页清除并重新填写。
AI_CREDENTIAL_ENCRYPTION_KEY=

# Task 9 备份/恢复脚本要求把本文件自身作为绝对 `--env-file`，并把绝对 BACKUP_DIR、项目根目录、Compose 文件、project 名、服务名、数据库名和保留数逐项显式传参；
# 脚本仅接受项目根目录内的严格子目录，归档以安全数据库标识命名，不能与 PostgreSQL 数据目录重叠或写入 NAS 其他项目。
BACKUP_DIR=/absolute/path/to/switch-price-monitor/backups
# BACKUP_RETENTION 只能是 1..10000；备份脚本以每数据库原子锁和 18 位单调 sequence 精确保留，mtime 仅供展示不得影响删除顺序。
BACKUP_RETENTION=14

# 请求体与进程关停上限使用十进制整数；前者单位字节，后者单位毫秒，避免无限内存或无限关停等待。
MAXIMUM_BODY_BYTES=1048576
SHUTDOWN_GRACE_MS=10000
